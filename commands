
To check the connectionsuse the below link in browser.
http://catalogue.alb-dev.haripalepu.cloud/health
http://catalogue.alb-dev.haripalepu.cloud/categories

Command to increase load
while true ; do curl http://catalogue.alb-dev.haripalepu.cloud/categories ; done

or login to the server and run this command "yes > /dev/null &"


Destroy command
terraform destroy -var-file=dev/dev.tfvars -var=app_version=1.0.0 -auto-approve
